{
  "name": "opsflow-erp-root",
  "version": "1.0.0",
  "description": "Root orchestrator for OpsFlow ERP + CRM Operations Portal",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend:dev": "cd backend && npm run dev",
    "backend:seed": "cd backend && npm run seed",
    "backend:migrate": "cd backend && npx prisma db push",
    "frontend:dev": "cd frontend && npm run dev",
    "dev": "npx concurrently \"npm run backend:dev\" \"npm run frontend:dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
